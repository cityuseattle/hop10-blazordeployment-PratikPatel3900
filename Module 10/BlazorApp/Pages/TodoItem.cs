/* by addding 
the root of the project (the TodoList folder) to hold a class 
that represents a todo item. */

/* root of todo project */
public class TodoItem
{
    public string Title { get; set;}
    public bool IsDone { get; set;}
}